const num = [1, 2, 3, 4, 5, 6, 7, 8,];
// const rev = num.reverse();
// console.log(rev);

const rev_num = [];

for (const num of numbers) {
  rev_num.unshift(num);
}
// console.log(rev_num);
const reversed_numbers = []